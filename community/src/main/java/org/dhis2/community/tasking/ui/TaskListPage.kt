package org.dhis2.community.tasking.ui

import androidx.compose.runtime.Composable

@Composable
fun TaskListPage(
    onTaskSelected: (String) -> Unit,
) {
    // Implementation of the task list page UI goes here
    // This will include displaying a list of tasks, handling task selection,
    // creating new tasks, marking tasks as completed, and deleting tasks.
}