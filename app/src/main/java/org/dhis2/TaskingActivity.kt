
package org.dhis2
/*
import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import org.dhis2.community.tasking.ui.tasks.di.TaskingFragment

class TaskingActivity : AppCompatActivity(){
    override fun onCreate(savedInstanceState: Bundle?){
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        val app = application as App
        val component = app.getTaskingComponent()
        val taskingFragment = TaskingFragment().apply {
            presenter = component.taskingPresenter()
            repository = component.taskingRepository()
        }

        supportFragmentManager.beginTransaction()
            .replace(R.id.fragment_container, taskingFragment)
            .commit()
    }
}*/
